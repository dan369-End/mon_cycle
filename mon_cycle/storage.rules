rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow users to upload and view their own profile image
    match /profile_images/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}